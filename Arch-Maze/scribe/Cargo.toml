[package]
name = "scribe"
version = "0.3.1"
edition = "2021"
description = "Disk scan + heatmap + select-to-backup TUI with a pluggable simulation hook"

[dependencies]
ratatui = "0.28"
crossterm = "0.28"
walkdir = "2"
serde_json = "1"

[profile.release]
opt-level = 2
